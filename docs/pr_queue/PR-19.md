# PR-19 Ops Alerts + Incident Timeline Export

## Scope
- Add alert routing adapters (Slack/email/webhook; configurable no-op for local).
- Add severity and dedupe windows for alert storms.
- Add incident timeline exporter from logs + decision snapshots + order intents.
- Produce incident bundle artifact for postmortem.

## Primary Files
- `monitoring/alerting.py`
- `monitoring/jsonl_logger.py`
- `scripts/daily_report.py`
- `reports/` tooling

## Verification
- `py -3 scripts/daily_report.py --db data/unified_trades.db --output-dir reports`
- `py -3 -m unittest tests_unified.test_pr_11_to_20_hardening.TestPR19IncidentExport -v`

## Codex Prompt
Task: Build operational alert routing with dedupe/severity and generate incident timeline exports from existing telemetry artifacts.
Acceptance: Tests validate dedupe behavior and incident export file creation with ordered event timeline.
Do not broaden trading permissions; keep live gated and off by default.
Prefer halting safely over trying to recover in ambiguous states.

