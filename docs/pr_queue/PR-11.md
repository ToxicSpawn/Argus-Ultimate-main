# PR-11 Soak Promotion Gate

## Scope
- Define hard pass/fail criteria for paper soak and pre-live soak.
- Block live enablement when soak criteria fail.
- Persist soak run summary (duration, error rate, timeout rate, reconciliation halts, duplicate intent count).
- Add CLI/report output for operator sign-off.

## Primary Files
- `ops/windows/Run-Soak.ps1`
- `scripts/daily_report.py`
- `monitoring/ops_metrics.py`
- `main.py`
- `core/config_manager.py`

## Verification
- `powershell -ExecutionPolicy Bypass -File ops/windows/Run-Soak.ps1 -Config unified_config.yaml -Cycles 60 -CycleSeconds 1.0 -Offline`
- `py -3 scripts/daily_report.py --db data/unified_trades.db --output-dir reports`
- `py -3 -m unittest tests_unified.test_pr_11_to_20_hardening.TestPR11SoakGate -v`

## Codex Prompt
Task: Implement PR-11 soak promotion gate. Add explicit soak SLO thresholds in config and enforce a promotion decision artifact (`PASS/FAIL`) after soak. Live mode must remain blocked if last soak gate fails.
Acceptance: Soak run emits a machine-readable gate result and human-readable summary. Add tests for pass and fail scenarios.
Do not broaden trading permissions; keep live gated and off by default.
Prefer halting safely over trying to recover in ambiguous states.
