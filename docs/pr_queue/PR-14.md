# PR-14 Portfolio VaR/CVaR + Cluster Drawdown Brakes

## Scope
- Add portfolio-level VaR/CVaR limits and hard stops.
- Add cluster-level drawdown brakes (majors/alts or config-driven clusters).
- Add dynamic leverage/exposure scaling by realized volatility + liquidity.
- Persist risk-limit hits to decision snapshots and metrics.

## Primary Files
- `risk/unified_risk_manager.py`
- `risk/position_sizing/cvar_based.py`
- `unified_trading_system.py`
- `unified_config.yaml`

## Verification
- `py -3 -m unittest tests_unified.test_pr_11_to_20_hardening.TestPR14PortfolioRisk -v`

## Notes
- VaR/CVaR, cluster drawdown, and vol/liquidity caps apply to risk-increasing trades.
- De-risking `SELL` flow remains allowed so limits do not trap exposure.

## Audit Query Examples
- `SELECT reason_code, COUNT(*) FROM decision_snapshots WHERE allowed = 0 GROUP BY reason_code ORDER BY COUNT(*) DESC;`
- `SELECT trace_id, reason_code, details_json FROM decision_snapshots WHERE reason_code IN ('PORTFOLIO_VAR_REJECT','PORTFOLIO_CVAR_REJECT','CLUSTER_DRAWDOWN_REJECT','VOL_LIQUIDITY_CAP_REJECT') ORDER BY timestamp DESC LIMIT 20;`

## Codex Prompt
Task: Implement portfolio VaR/CVaR guardrails with cluster drawdown brakes and volatility-adjusted leverage caps. Enforce these before intent creation.
Acceptance: Tests show trades are blocked when portfolio or cluster limits are exceeded and reason codes are persisted.
Do not broaden trading permissions; keep live gated and off by default.
Prefer halting safely over trying to recover in ambiguous states.
