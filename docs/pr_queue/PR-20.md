# PR-20 Secrets Vault + Key Hygiene

## Scope
- Move exchange/API secrets to vault-backed provider interface.
- Add key-permission verification at startup (trade/read/withdraw flags).
- Add key rotation metadata checks and age warnings.
- Block startup when insecure key policy violations are detected.

## Primary Files
- `core/config_manager.py`
- `core/config_unified.py`
- `main.py`
- `security/` (new provider abstractions)

## Verification
- `py -3 main.py validate --config unified_config.yaml`
- `py -3 -m unittest tests_unified.test_pr_11_to_20_hardening.TestPR20SecretsPolicy -v`

## Codex Prompt
Task: Implement secrets-provider abstraction and startup key hygiene policy checks. Validation must fail fast on over-permissioned or expired/old keys.
Acceptance: Tests cover valid policy pass and policy fail paths with clear error messaging.
Do not broaden trading permissions; keep live gated and off by default.
Prefer halting safely over trying to recover in ambiguous states.
