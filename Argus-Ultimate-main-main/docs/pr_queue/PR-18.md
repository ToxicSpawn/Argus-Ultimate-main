# PR-18 Champion-Challenger + Canary Rollout

## Scope
- Add strategy/model profile registry with champion/challenger metadata.
- Add canary deployment mode (small capped capital allocation).
- Add rollback triggers (drawdown, error rate, slippage deterioration, recon faults).
- Record promotions, canary outcomes, and rollbacks in audit logs.

## Primary Files
- `research/walk_forward_harness.py`
- `deploy/` bundle modules
- `unified_trading_system.py`
- `unified_config.yaml`

## Verification
- `py -3 -m unittest tests_unified.test_pr_11_to_20_hardening.TestPR18CanaryPromotion -v`

## Codex Prompt
Task: Implement champion/challenger registry and canary rollout policy with automatic rollback triggers. Enforce capped capital for challenger until promotion criteria pass.
Acceptance: Tests show challenger starts in canary, fails trigger, and rolls back to champion automatically.
Do not broaden trading permissions; keep live gated and off by default.
Prefer halting safely over trying to recover in ambiguous states.

