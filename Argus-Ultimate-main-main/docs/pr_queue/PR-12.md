# PR-12 Fail-Closed Exchange Adapter

## Scope
- Add a single exchange adapter layer for all venue calls.
- Standardize timeout, retry, circuit-breaker, and cancel semantics.
- Introduce explicit ambiguous state path (`SENT -> UNKNOWN -> RECON_REQUIRED`).
- Prevent new intents for symbols/accounts in ambiguous state until reconciled.

## Primary Files
- `unified_execution_engine.py`
- `execution/state_store.py`
- `execution/reason_codes.py`
- `utils/circuit_breaker.py`

## Verification
- `py -3 -m unittest tests_unified.test_pr_11_to_20_hardening.TestPR12FailClosedAdapter -v`

## Codex Prompt
Task: Implement a fail-closed exchange adapter with unified timeout/retry behavior and explicit ambiguous outcome handling. If order status is unknown after timeout, block further exposure increases and require reconciliation.
Acceptance: Add deterministic tests for timeout, retry, and ambiguous-state lockout. Show that no duplicate exposure is created.
Do not broaden trading permissions; keep live gated and off by default.
Prefer halting safely over trying to recover in ambiguous states.
