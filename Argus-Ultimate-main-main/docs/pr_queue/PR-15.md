# PR-15 Execution Quality Feedback Loop

## Scope
- Track realized slippage vs benchmark per symbol/venue/strategy.
- Auto-tune maker/taker fallback timeout and slice size from recent quality metrics.
- Penalize routes with persistent adverse selection.
- Record parameter changes with audit trail.

## Primary Files
- `unified_execution_engine.py`
- `execution/execution_models.py`
- `monitoring/ops_metrics.py`
- `monitoring/trade_ledger.py`

## Verification
- `py -3 -m unittest tests_unified.test_pr_11_to_20_hardening.TestPR15ExecutionFeedback -v`

## Inspect
- Decision snapshots (latest tuned execution plans):
  - `SELECT trace_id, reason_code, execution_plan_json FROM decision_snapshots ORDER BY timestamp DESC LIMIT 20;`
- Tuning audit trail:
  - `SELECT stage, payload_json FROM decision_events WHERE stage = 'execution_quality_tuning' ORDER BY timestamp DESC LIMIT 20;`

## Codex Prompt
Task: Add an execution quality feedback controller that updates routing and slicing parameters based on observed slippage/fill quality by symbol and venue.
Acceptance: Deterministic test shows poor maker fills increase taker fallback aggressiveness and are reflected in stored plan parameters.
Do not broaden trading permissions; keep live gated and off by default.
Prefer halting safely over trying to recover in ambiguous states.
